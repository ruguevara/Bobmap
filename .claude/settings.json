{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); if echo \"$file\" | grep -q '^.*/src/'; then node /Users/ruguevara/projects/Bobiverse/scripts/screenshot.js 2>&1 || true; fi",
            "timeout": 30,
            "statusMessage": "Taking screenshot..."
          }
        ]
      }
    ]
  }
}
